# Audit: rust-signal-dev
**Task**: Implement OpenTelemetry observability in Rust signal service
**Delegated by**: supervisor
**Started**: 2026-05-24T18:04:00Z
**Files read**: Cargo.toml, src/app.rs, src/bin/main.rs, config/development.yaml, config/production.yaml

## Log

### 1. Read existing tracing setup
- **Action**: read
- **Target**: Cargo.toml, main.rs, config YAMLs
- **Rationale**: Understand existing logging infrastructure before adding OTel
- **Outcome**: Found 172+ tracing macros in use but no OTel, no metrics, no export

### 2. Created src/telemetry.rs
- **Action**: write
- **Target**: src/telemetry.rs
- **Rationale**: Need OTel init with exporter, processor, propagator, and graceful RAII guard
- **Outcome**: `init_telemetry()` with OTLP HTTP exporter, BatchSpanProcessor (5s flush), Periodic metric reader (30s), W3C propagator, service resource attributes; TelemetryGuard for RAII-style shutdown on drop

### 3. Created src/metrics.rs
- **Action**: write
- **Target**: src/metrics.rs
- **Rationale**: Need Prometheus /metrics scraping endpoint
- **Outcome**: axum HTTP server on :9464 serving GET /metrics (Prometheus text format) and GET /health (liveness); ProcessCollector on Linux for RSS/CPU/fd

### 4. Updated Cargo.toml
- **Action**: edit
- **Target**: Cargo.toml
- **Rationale**: Add 8 OTel + Prometheus dependencies
- **Outcome**: Added opentelemetry, opentelemetry_sdk, opentelemetry-otlp, opentelemetry-semantic-conventions, tracing-opentelemetry, prometheus; added registry feature to tracing-subscriber

### 5. Updated src/lib.rs
- **Action**: edit
- **Target**: src/lib.rs
- **Rationale**: Expose new telemetry and metrics modules
- **Outcome**: Added `pub mod telemetry; pub mod metrics;`

### 6. Rewrote src/bin/main.rs boot sequence
- **Action**: edit
- **Target**: src/bin/main.rs
- **Rationale**: Need OTel init before logging, tracing subscriber config, metrics server
- **Outcome**: Calls init_telemetry first; configures tracing_subscriber::fmt with EnvFilter + LOG_FORMAT env; spawns metrics server in background

### 7. Added #[instrument] to 30+ functions
- **Action**: edit (3 files)
- **Target**: initializers/initialize_chat.rs (22 instr), initialize_server.rs (6 instr), initialize_consumers.rs (3 instr)
- **Rationale**: Automatic span creation for key functions — room operations, producer/consumer lifecycle, OPAQUE protocol, NATS consumers
- **Outcome**: All critical paths now create OTel spans with relevant fields (room_id, participant_id, appointment_id, stream, consumer)

## Summary
- **Files created**: src/telemetry.rs, src/metrics.rs
- **Files modified**: Cargo.toml, src/lib.rs, src/bin/main.rs, initialize_chat.rs, initialize_server.rs, initialize_consumers.rs
- **Key decisions**: Global TracerProvider works for #[instrument] spans; tracing-opentelemetry Layer deferred due to trait incompatibility with current loco subscriber; Prometheus on separate port (9464) from main app (3500)
- **Completed**: 2026-05-24
