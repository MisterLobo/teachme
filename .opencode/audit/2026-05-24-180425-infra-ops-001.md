# Audit: infra-ops
**Task**: Fix OTel collector, Prometheus targets, Docker Compose monitoring, Loki stack
**Delegated by**: supervisor
**Started**: 2026-05-24T18:04:00Z
**Files read**: otel-collector-config.yaml, prometheus.yaml, docker-compose.yaml, docker/loki/alloy-local-config.yaml, docker/loki/docker-compose.yaml, docker/loki/loki-config.yaml

## Log

### 1. Read existing infra config files
- **Action**: read
- **Target**: All 6 observability config files
- **Rationale**: Understand current state before making changes
- **Outcome**: Found broken OTel exporter, minimal Prometheus targets, no exporters in docker-compose

### 2. Fixed otel-collector-config.yaml
- **Action**: write
- **Target**: otel-collector-config.yaml
- **Rationale**: Pipeline referenced `otlp` exporter that didn't exist; missing batch/memory_limiter/resource processors
- **Outcome**: Rewrote with batch (1s timeout, 1024 batch), memory_limiter (512MiB), resource processor, and valid `debug` exporters

### 3. Rewrote prometheus.yaml
- **Action**: write
- **Target**: prometheus.yaml
- **Rationale**: Only scraped self and node-exporter; no application service targets
- **Outcome**: Added scrape targets for Go backend (:7890/metrics), NestJS (:3000/metrics), Rust signal (:3500/metrics), Python AI (:8800/metrics), postgres-exporter (:9187), redis-exporter (:9121), NATS (:8222/metrics), OTel collector (:8888). Fixed scrape interval from 1m to 15s.

### 4. Updated docker-compose.yaml
- **Action**: edit
- **Target**: docker-compose.yaml
- **Rationale**: Need OTEL env vars on all services; need redis/postgres exporters; prometheus needs network access to services
- **Outcome**: Added redis-exporter, postgres-exporter containers; added monitoring network to postgres/nats/redis; added OTEL env vars to app services; switched to otel-contrib collector image

### 5. Rewrote docker/loki/alloy-local-config.yaml
- **Action**: write
- **Target**: docker/loki/alloy-local-config.yaml
- **Rationale**: Original scraped only flog container; needed to discover all Docker containers + accept OTLP from main stack
- **Outcome**: Generalized to all Docker containers (relabel with container/image/compose_project/service labels); added OTLP receiver on :4317/:4318 with batch processing

### 6. Updated docker/loki/docker-compose.yaml
- **Action**: edit
- **Target**: docker/loki/docker-compose.yaml
- **Rationale**: Alloy OTLP ports needed exposure for cross-stack integration
- **Outcome**: Exposed 4317 (gRPC) and 4318 (HTTP) on Alloy service

## Summary
- **Files created**: none
- **Files modified**: otel-collector-config.yaml, prometheus.yaml, docker-compose.yaml, docker/loki/alloy-local-config.yaml, docker/loki/docker-compose.yaml
- **Key decisions**: Two-stack topology (main docker-compose for databases + Prometheus/Grafana/OTEL; Loki stack standalone with Alloy); OTEL env vars added but not enabled by default
- **Completed**: 2026-05-24
