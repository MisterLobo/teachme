# Audit: go-backend-dev
**Task**: Implement full OpenTelemetry observability in apps/dbal
**Delegated by**: supervisor
**Started**: 2026-05-24T18:04:00Z
**Files read**: go.mod, src/boot/grpc.go, src/main.go, src/config/config.go, src/boot/boot.go

## Log

### 1. Read existing OTel and prometheus code
- **Action**: read
- **Target**: go.mod, grpc.go, main.go
- **Rationale**: Understand what's already wired and what's broken
- **Outcome**: Found OTel gRPC stats handler wired but no TracerProvider; Prometheus metrics wired but with empty histogram and placeholder metric names; `/metrics` endpoint missing

### 2. Created src/telemetry/otel.go
- **Action**: write
- **Target**: src/telemetry/otel.go
- **Rationale**: Need centralized OTel boot with TracerProvider, MeterProvider, exporter, propagator
- **Outcome**: 174-line InitOTel function with OTLP HTTP exporter, BatchSpanProcessor (5s batch, 512 max), W3C TraceContext + Baggage propagators, configurable sampler from env

### 3. Created src/telemetry/metrics.go
- **Action**: write
- **Target**: src/telemetry/metrics.go
- **Rationale**: Need proper Prometheus metrics endpoint (was missing), fix bugs
- **Outcome**: MetricsServer on :9464 with DefaultRegistry (Go runtime + process collectors); fixed histogram from `[]float64{}` to meaningful buckets; renamed placeholder `"yeah"/"sure"` to `"grpc_panics_total"`; OpenMetrics content-type

### 4. Created src/telemetry/fiber.go
- **Action**: write
- **Target**: src/telemetry/fiber.go
- **Rationale**: Fiber HTTP server needs OTel middleware for trace context extraction from incoming HTTP requests
- **Outcome**: Fiber handler that extracts W3C traceparent, creates server span with HTTP attributes (method, path, route, status, user_agent), stores span context in request context for downstream propagation

### 5. Created src/telemetry/nats.go
- **Action**: write
- **Target**: src/telemetry/nats.go
- **Rationale**: NATS publish/subscribe needs trace context propagation to maintain distributed traces across services
- **Outcome**: Wrapper functions for sync and async JetStream publish that inject trace context into NATS headers; WrapMsgHandler that extracts trace context from received messages and creates consumer spans

### 6. Modified src/boot/grpc.go
- **Action**: edit
- **Target**: src/boot/grpc.go
- **Rationale**: Replace buggy inline Prometheus setup with telemetry package
- **Outcome**: Removed direct `prometheus.NewRegistry()`/`promauto` setup; replaced with `telemetry.GRPCServerMetrics()`; removed unused imports

### 7. Modified src/main.go
- **Action**: edit
- **Target**: src/main.go
- **Rationale**: Bootstrap OTel, add Fiber middleware, graceful shutdown
- **Outcome**: Added OTel init after boot context; added Fiber OTel middleware to middleware chain; started Prometheus metrics server; added graceful shutdown for metrics + OTel (10s timeout)

### 8. Patched NATS trace propagation across codebase
- **Action**: edit (4 files)
- **Target**: workers/payment.worker.go, services/auth.impl.go, lib/nats.go, boot/nats.go
- **Rationale**: Replace raw NATS publish/subscribe with telemetry-wrapped versions
- **Outcome**: All NATS publishes now carry W3C trace context; all subscriptions extract and continue traces

## Summary
- **Files created**: src/telemetry/otel.go, metrics.go, fiber.go, nats.go
- **Files modified**: src/boot/grpc.go, src/main.go, workers/payment.worker.go, services/auth.impl.go, lib/nats.go, boot/nats.go
- **Key decisions**: Separate metrics port :9464 (Prometheus convention); Telemetry package as single import for all OTel concerns
- **Problems fixed**: Empty histogram buckets (bug), placeholder metric names (bug), missing /metrics endpoint, no TracerProvider
- **Completed**: 2026-05-24
